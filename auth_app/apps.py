"""Application configuration for the authentication app."""

from django.apps import AppConfig


class AuthAppConfig(AppConfig):
    """Configure the authentication Django app."""

    name = 'auth_app'
