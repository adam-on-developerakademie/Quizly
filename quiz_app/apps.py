"""Application configuration for the quiz app."""

from django.apps import AppConfig


class QuizAppConfig(AppConfig):
    """Configure the quiz Django app."""

    name = "quiz_app"
    verbose_name = "Quiz"
